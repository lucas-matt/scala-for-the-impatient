package exercise9

import org.scalatest.FlatSpec
import org.scalatest.matchers.ShouldMatchers

/**
 * Created with IntelliJ IDEA.
 * User: lucas
 * Date: 7/29/13
 * Time: 8:22 PM
 * To change this template use File | Settings | File Templates.
 */
class Solutions extends FlatSpec with ShouldMatchers {

  "(1) This snippet" should "reverse the lines in a file (making the last line the first, and so on)" in {
    fail()
  }

  "(2) This snippet" should "read a file with tabs and replace them with spaces so that tab stops are at n-column boundaries" in {
    fail()
  }

  "(3) This snippet" should "read a file and print all words with more than 12 characters to the console (can be done in single line)" in {
    fail()
  }

  "(4) Our program" should "read a file containing only floating point numbers and print the sum, avg, min and max of the numbers in the file" in {
    fail()
  }

  "(5) Our program" should """write the powers of 2 and their reciprocals to a file, with the exponent ranging from 0 to 20, such as :

       1    1
       2    0.5
       4    0.25

  """ in {
    fail()
  }

  "(6) Our program" should "use a regex to search for double quoted strings in a source file" in {
    fail()
  }

  "(7) Our program" should "read a file and print all tokens in that file that are not floating point numbers, using a regex" in {
    fail()
  }

  "(8) Our program" should "print the src attributes of all img tags of a web page (using a regex)" in {
    fail()
  }

  "(9) Our program" should "count how many files with .class are in a given directory tree" in {
    fail()
  }

  "(10) Given the example with a serializable Person and we" should """demonstrate serialization by :

        - constructing a few Person objects
        - making some of them friends
        - and then saving an Array[Person] to a file
        - reading that array back in
        - and ensuring that the relationships persisted

  """ in {
    fail()
  }

}
